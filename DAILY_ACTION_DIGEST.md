# 일일 매장 마감보고 액션 다이제스트

매장별 오픈마감보고(Slack, `verish-offline` 워크스페이스, [[API_INTEGRATION.md]] 4부 참고)에서
**본사가 액션을 취해야 하는 항목**과 **핵심 KPI 증감**을 매일 아침 요약 공유하는 워크플로우.

## 1. 목적

원본 마감보고는 매출/랭킹/VOC가 전부 섞인 긴 텍스트라 본사에서 매일 전체를 읽기 어렵다.
그중 **조치가 필요한 정성적 내용**을 4개 카테고리로 뽑고, **핵심 정량 지표(KPI)**를 전주
동요일과 비교해 매장별 + 전체 합산본으로 공유한다.

**daily는 액션 가능한 정보만.** 매일 봐야 신호가 살아있는 정보(안전 이슈, 급변 KPI, 전사 판매
급증 TOP5 등)만 daily에 남기고, 매장 단위 세부 breakdown처럼 노이즈 대비 액션 가치가 낮거나
주간 단위로 봐도 충분한 정보는 daily에서 뺀다(예: 3.2의 매장별 판매 급증 breakdown 제외 결정).

## 2. 정성 액션 카테고리 정의

| 카테고리 | 포함 기준 | 예시 |
|---|---|---|
| 🚨 **안전** | 고객·직원 안전에 직접 영향, 시설 결함, 사고 위험 | 자동문 오작동, 누수/단수, 화재/전기 이상, 고객 낙상 등 사고 발생 |
| 👥 **방문객 영향** | 입점객 수·동선·매장 혼잡도에 영향을 주거나, 방문객 유입 자체를 막는 요인 | 피크타임 과밀/웨이팅, 매장 위치 식별 어려움, 유동인구 급감/급증 추세 |
| 🎯 **전환율 영향** | 피팅→구매 전환에 영향을 주는 요인 (긍정/부정 모두, 특히 급변 시) | 특정 상품 품절로 인한 구매 포기, 전일 대비 전환율 급락, 응대 지연 |
| ⚠️ **기타 부정 VoC** | 위 3개에 안 들어가지만 빠르게 확인이 필요한 부정적 이슈 | 제품 불량/하자, 타 매장/직원 관련 불만, 패키징 혼동, 가격 경쟁력 이슈, 본사-매장 커뮤니케이션 누락 |

**뽑지 않는 것**: 단순 매출 수치, 판매 랭킹, 근태 보고, 긍정적이기만 한 일반 VOC(액션이 필요 없는 것).
같은 이슈가 여러 매장에서 동시에 나오면(예: 특정 SKU 품절) 합산본에서 "복수 매장 공통 이슈"로 승격.

## 3. 정량 지표(KPI) 정의

| 지표 | 정의 | 데이터 소스 |
|---|---|---|
| 방문객수 | mAsh "일별 매장 방문 횟수" | [[api-integration]] 2부 (mAsh) |
| 전환율 | 영수증 건수 ÷ 방문객수 | PLAY MD `getSalesDetailInfo`(영수증 건수) ÷ mAsh(방문객수) |
| 객단가 (AOV) | 실결제액 ÷ 영수증 건수 | PLAY MD `getSalesDetailInfo` |
| 실결제액 | `totalPaymentPrice` 합계 | PLAY MD `getSalesDetailInfo` |

비교 기준일은 **전주 동요일**(대상일 − 7일)이며, 매장별 + 전체 합계 두 단위로 산출한다.
구현: [src/report/daily-kpi-digest.ts](src/report/daily-kpi-digest.ts) (`npm run kpi-digest [날짜]`).

**전환율의 증감 표기는 bps(basis point) 단위를 쓴다(2026-07-05 확정).** 전환율은 그 자체가
비율(%)이라 다른 지표처럼 "전주 대비 상대 증감률(%)"을 쓰면 %의 %라 헷갈린다(예: 13.4%→16.8%가
"+3.4%"로 찍히면 3.4%p 오른 것으로 착각하기 쉬움). 그래서 전환율 값 자체는 그대로 %로 표기하되,
전주 대비 변화는 **절대 %p 차이를 bps로 환산**해서 보여준다(1%p = 100bps, 13.4%→16.8%면 "+340bps").
방문객수·객단가·실결제액은 비율 지표가 아니므로 지금처럼 상대 증감률(%)을 그대로 쓴다.
구현: `bpsChange()`/`fmtBps()` in [src/report/daily-kpi-digest.ts](src/report/daily-kpi-digest.ts).

### 3.1 증감 플래그 기준 (⚠️)

지표별로 자연 변동폭이 달라 기준을 다르게 둔다. **전환율의 플래그 판정은 화면에 찍히는 bps 값이
아니라 내부적으로 계산한 상대 증감률(%) 기준을 그대로 쓴다** — 표시 단위만 bps로 바뀌었을 뿐 "비정상
변동"을 가리는 기준 자체는 안 바꿨다:

| 지표 | 플래그 기준 |
|---|---|
| 객단가 | ±15% (변동이 상대적으로 작은 지표) |
| 방문객수 / 전환율(상대 증감률 기준) / 실결제액 | ±25% (요일·날씨·이벤트 영향을 크게 받는 지표) |

전주 동요일 데이터가 없어 비교 불가한 경우 "N/A"로 표기하고 플래그하지 않는다.

### 3.2 판매 급증 상품 기준

특정 상품이 전일(대상일) 갑자기 많이 팔렸는지를 **전체(전사) 합산** 단위로만 잡아낸다.
저볼륨 SKU 특성상 하루 1~2개 차이만으로도 퍼센트가 크게 튀므로, 비율과 절대 판매량 두 조건을 동시에 요구한다.

| 조건 | 기준 |
|---|---|
| 최소 판매량 (대상일) | 5개 이상 |
| 증가율 (전주 동요일 대비) | 2배(+100%) 이상 |
| 표기 개수 | 상위 5건 (초과분은 "그 외 N건 더") |

- 집계 단위는 `productCode`(색상/사이즈 통합) 기준 판매수량(`qty`) 합계다.
- 전주 동요일 판매량이 0(또는 환불 초과로 순수량이 음수인 경우 0으로 처리)이면 비율 계산이 불가하므로 **"신규 급증"**으로 표기한다.
- 비교 기준일은 KPI와 동일하게 **전주 동요일**을 사용한다(별도 베이스라인 없음).
- 구현: [src/report/daily-kpi-digest.ts](src/report/daily-kpi-digest.ts)의 `SPIKE` 상수 및 `findSpikes()` — KPI와 같은 명령(`npm run kpi-digest [날짜]`)으로 함께 출력된다.
- **매장별 breakdown은 daily에서 뺐다(2026-07-05).** 매장 단위로 쪼개면 대부분의 매장에서 매일 몇 건씩
  걸려 27행 안팎의 표가 나왔는데, 그중 실제로 조치가 필요한 경우는 드물고 정상적인 판매 편차인 경우가
  대부분이었다(예: 성수점 "베리시 쇼핑백"처럼 비의류 소모품이 조건에 걸리는 등). 전사 합산 TOP5는
  재입고 우선순위 판단에 바로 쓸 수 있는 신호라 daily로 남기고, 매장별 세부 내역이 필요하면
  `qtyByProduct`에 `shopCode` 필터를 다시 추가해 임시로 조회하거나(과거 구현 참고), 주간 리포트로
  넘기는 걸 권장한다.

## 4. 출력 형식

### 4.1 KPI 섹션 (합산 + 매장별 표)

```
## 📊 정량 지표 (<날짜> vs 전주 <요일>)

*괄호 안 수치는 전주 동요일 대비 증감(전환율은 bps, 나머지는 %)*

**전체 합계 (전주 동요일 대비)**
방문객수: N,NNN명 (+N.N%)
전환율: NN.N% (+Nbps)
객단가: NN,NNN원 (+N.N%)
실결제액: NNN,NNN,NNN원 (+N.N% ⚠️)

**매장별 (전주 동요일 대비)**
| 매장 | 방문객수 | 전환율 | 객단가 | 실결제액 |
|---|---|---|---|---|
| <매장명> | N,NNN (+N.N%) | NN.N% (+Nbps) | NN,NNN원 (+N.N%) | N,NNN,NNN원 (+N.N% ⚠️) |
...
```

### 4.2 판매 급증 상품 섹션 (전체 합산)

불릿 나열은 숫자 비교가 눈에 잘 안 들어와서(2026-07-05 확인) **표 형식**으로 낸다.
매장별 표는 daily에서 뺐다(3.2 참고) — 노이즈 대비 액션 가치가 낮았다.

```
## 🚀 판매 급증 상품 (전주 동요일 대비 2배 이상 · 최소 5개 이상 판매)

| 상품 | 오늘 | 전주 | 배수 |
|---|---|---|---|
| <상품명> (<상품코드>) | N개 | N개 | N.N배 / 신규 급증 |
...
```

전체 합산에 오른 상품이 특정 매장(들)에서 특히 도드라지면, 정성 액션의 "복수 매장 공통 이슈"(4.3)에서
같은 방식으로 언급해도 된다(예: 재입고 필요성 강조) — 이건 VOC를 읽으며 사람이 판단하는 영역이라
스크립트 출력과는 별개다.

### 4.3 정성 액션 — 합산(전체) 섹션

```
## 📋 정성 액션 요약 (전체)

🚨 *안전 — 즉시 조치 필요 매장: N곳*

• *<매장명>*: 한 줄 요약

🔁 *복수 매장 공통 이슈*

• <이슈> — 해당 매장: A, B, C

🎯 *전환율 임팩트 TOP 3*

1. <내용>

2. <내용>

3. <내용>
```

### 4.4 정성 액션 — 매장별 섹션

카테고리 4줄은 각각 빈 줄로 구분한다 (Slack/마크다운 렌더링 시 한 문단으로 뭉치는 것 방지).
항목이 여러 개면 `•`로 같은 줄 안에서 구분, 없으면 "특이사항 없음"/"없음"이라고 명시한다.

```
### <매장명>

🚨 안전: (특이사항 없음 / 내용)

👥 방문객 영향: (특이사항 없음 / 내용)

🎯 전환율 영향: (특이사항 없음 / 내용)

⚠️ 기타 부정 VoC: (없음 / 내용)
```

**4개 카테고리가 전부 "특이사항 없음"/"없음"인 매장은(2026-07-05부터) 아래처럼 한 줄로 압축한다**
— 정보량이 0인데 매일 4줄씩 자리를 차지해서 daily 가독성을 해쳤다:

```
### <매장명>

이상 없음
```

카테고리 중 하나라도 실제 내용이 있으면(안전 이슈, 방문객 영향, 전환율 영향, 부정 VoC 중 무엇이든)
압축하지 않고 4줄 풀 포맷을 그대로 쓴다.

### 4.5 전체 배치 순서

**2026-07-05부터: 제목 메시지 + 스레드 답글 2단 구성.** 피드가 길어지는 걸 막기 위해 채널 메인에는
짧은 제목 한 줄만 남기고, 실제 본문(KPI+정성 전체)은 그 메시지의 스레드 답글로 붙인다:

1. 채널 메인에 볼드체 한 줄만 전송: `**📋 Verish offline daily report (<날짜>)**`
2. 그 메시지의 `message_ts`를 `thread_ts`로 써서 같은 스레드에 답글을 전송. 답글 맨 앞에
   `<!subteam^S08ESRKCQLD>`(`@sales-offline` 팀 태그)를 넣고, 빈 줄 하나 띄운 뒤 KPI 섹션(4.1) →
   판매 급증 상품(4.2) → 정성 액션 합산(4.3) → 매장별 정성 액션(4.4) 순으로 본문을 이어 붙인다.
3. 답글 본문이 5000자(Slack 메시지당 텍스트 한도)를 넘으면 섹션 경계에서 끊어 같은 스레드에
   여러 개의 답글로 나눠 보낸다. `@sales-offline` 태그는 첫 번째 답글에만 붙인다.

(2026-07-04까지는 `# 📋 Verish offline daily report (<날짜>)`를 본문 맨 위 제목으로 포함한 메시지
하나를 채널에 통째로 보냈다. 2026-07-05 첫 실제 발행 이후 피드 길이 문제로 이 2단 구성으로 바꿨다 —
7절 테스트 이력 참고.)

## 5. 워크플로우

| 항목 | 내용 |
|---|---|
| 트리거 | 전일 각 매장 마감보고 게시 완료 후 |
| 발행 시각 | 익일 오전 9시 |
| 정성 데이터 소스 | `reports/store-voc-log.json` (전일 `date` 기준 필터) — `npm run collect-voc`로 최신화 후 `npm run voc-for-date <날짜>`로 매장별 마감보고 원문 추출 (순수 오프라인 데이터, Shopify 미포함) |
| 정량 데이터 소스 | `npm run kpi-digest`(PLAY MD + mAsh 실시간 조회, 별도 캐시 없음) |
| 대상 채널 | `deepdive-global.slack.com` 워크스페이스의 **#part-offline-비공개** (channel_id: `C08M47Y7HA9`) |
| 대상 채널 접근 방법 | claude.ai에 이미 연결된 표준 Slack MCP 커넥터 사용 (이 채널은 `deepdive-global`이라 [[API_INTEGRATION.md]] 4부의 `verish-offline` 전용 Bot Token 클라이언트와는 다른 연결) |
| 발행 구조 | 채널 메인엔 볼드 한 줄 제목만, 본문은 스레드 답글로(4.5 참고) — 답글 첫 줄에 `<!subteam^S08ESRKCQLD>`(`@sales-offline`) 태그 |
| 정성 분류 방식 | 원문 텍스트를 사람(또는 Claude)이 읽고 카테고리별로 분류·요약 — 아직 자동 파싱/분류 스크립트는 없음 |
| 스케줄 | `verish-offline-daily-digest` (매일 09:00 KST, `C:\Users\edgar\.claude\scheduled-tasks\verish-offline-daily-digest\SKILL.md`) |

## 6. 알려진 한계

- **자동 분류 미구현.** 정성 액션 부분은 지금 원문을 읽고 수동으로 카테고리에 배정한다. 문구 패턴이
  안정화되면(예: `[안전]` 섹션은 거의 항상 그대로 가져오면 됨) 스크립트화 가능.
- **중간관리자 채널은 노이즈가 있다** ([[API_INTEGRATION.md]] 4.4 참고) — 마감보고 형식이 아닌
  일반 대화가 섞여 있어 해당 매장은 실제 마감보고 메시지만 골라 읽어야 한다.
- **mAsh 방문객 데이터는 다음날 오전 5시경 갱신된다.** 발행 시각(오전 9시)에는 문제없지만, 그보다
  이른 시간에 수동으로 `kpi-digest`를 돌리면 최근 1~2일치 방문객수가 0/N/A로 나올 수 있다.
- **mAsh는 시작일=종료일인 하루짜리 날짜 범위 요청 시 빈 배열을 반환한다** (`daily-kpi-digest.ts`에서
  종료일에 +1일을 더해 조회한 뒤 원하는 날짜만 필터링해서 우회함).
- **Shopify 크로스채널 검증은 daily 루틴에서 제외한다 (2026-07-07 결정).** 한동안 특정 상품 품절이
  여러 매장에서 동시에 보고되면 [[COUNTRY_CHANNEL_ANALYSIS.md]]의 Shopify 재고 상태와 대조해 언급했으나,
  daily는 순수 오프라인 데이터만 다루기로 범위를 좁혔다(속도 개선 목적도 있음 — 아래 성능 개선 메모 참고). Shopify와의
  온·오프라인 재고 대조가 필요하면 별도 요청으로 [[COUNTRY_CHANNEL_ANALYSIS.md]] 방법론을 그때 사용한다.
- **판매 급증 상품 필터링은 `productCode` 단위라 쇼핑백 등 비(非)의류 품목도 이론적으로 걸릴 수 있다.**
  (매장별 breakdown을 뺀 이후엔 전사 합산 TOP5에서만 해당 — 실제로 2026-07-03 성수점 쇼핑백 사례는
  매장 단위에서만 걸렸고 전사 합산에서는 비율 조건 미충족으로 자동으로 빠졌다.) 조건에는 맞지만
  액션이 필요 없는 항목이 TOP5에 오르면 수동으로 걸러서 보고에서 제외한다(자동 화이트/블랙리스트는
  아직 없음).
- **시간대별 세부 수치(예: 매장이 자체 집계한 시간대별 구매전환율)를 인용해 "최고/최저"라고 단정할 때는
  반드시 전체 시간대 수치를 다시 훑어 비교한 뒤에 쓴다.** 2026-07-04 신제주 발행분에서 "21시 이후
  34.4%가 최고"라고 잘못 적었다가(실제로는 11~12시 44.4%, 18~21시 40.3%가 더 높음) 발행 후
  스레드 정정을 올린 사례가 있다 — 한 항목만 보고 최상급 표현을 쓰지 말 것.
- **발행 전 처리 속도 개선 메모(2026-07-05, 발행이 느리다는 피드백 반영):**
  - **중복 발송 확인(-1단계)**: `slack_read_channel`을 limit 20으로 통째로 불러오면 무관한 장문
    메시지(스레드·포워딩 포함)까지 전부 로드돼 느리고 토큰도 많이 쓴다. 가능하면
    `slack_search_public_and_private`로 `"Verish offline daily report (<날짜>)"` 제목을 직접
    검색하는 편이 훨씬 빠르다 — 다음 실행부터는 검색 우선, 검색 결과가 애매할 때만 채널 히스토리로
    폴백.
  - **VOC 원문 추출**: node 스크립트 결과를 임시 파일에 덤프한 뒤 별도 Read 호출로 다시 읽는 2단계
    왕복은 불필요하다. 스크립트가 stdout으로 바로 출력하게 하고 Bash 도구 결과에서 곧바로 읽으면
    호출 한 번을 아낄 수 있다. **(2026-07-07 스크립트화 완료)** `npm run voc-for-date <대상일>`
    ([src/report/voc-for-date.ts](src/report/voc-for-date.ts))이 날짜 필터링 → 매장별 그룹핑 →
    `:pushpin:` 포함 마감보고 원문 판별(여러 건이면 최장문 선택) → stdout 출력까지 한 번에 처리한다.
    이전에는 ad hoc `node -e` 스크립트로 미리보기 조회 1회 + 임시 파일 덤프 1회 + Read 1회, 총
    3회 왕복이 들었는데 이제 Bash 호출 1회로 끝난다.
  - **Shopify 크로스채널 검증 제거**: daily 루틴 범위를 순수 오프라인 데이터로 좁히면서(위 크로스채널
    검증 항목 참고) `COUNTRY_CHANNEL_ANALYSIS.md` 조회·대조 단계 자체가 사라져 그만큼 처리 시간이
    줄었다.

## 7. 테스트 이력

| 날짜 | 대상 데이터 | 상태 |
|---|---|---|
| 2026-07-04 | 9개 매장 마감보고 (정성만) | 양식 확정용 1차 드래프트 작성 (Slack 미발행, 사용자 검토용) |
| 2026-07-04 | 동일 | 카테고리 줄 사이 빈 줄 추가 (렌더링 시 한 문단으로 뭉치는 문제 수정) |
| 2026-07-04 | 동일 | 타이틀을 "Verish offline daily report"로 확정, DM(구성회_세일즈)으로 2회 전송 |
| 2026-07-05 | 2026-07-03 (정성+KPI 결합) | KPI 섹션 추가 (mAsh 하루짜리 범위 버그 발견·우회), 최종 통합 포맷으로 DM 전송 — [reports/daily-digest-2026-07-03-draft.md](reports/daily-digest-2026-07-03-draft.md) |
| 2026-07-05 | 동일 | KPI 수치에 "전주 동요일 대비" 라벨 명시 추가 (섹션 제목 + 캡션), 재전송 |
| 2026-07-05 | 2026-07-03 (KPI 스크립트) | 판매 급증 상품 섹션 추가 (전체 합산 + 매장별, `daily-kpi-digest.ts`에 통합) — 기준 정의는 3.2 참고 |
| 2026-07-05 | 동일 | 판매 급증 상품 섹션을 불릿 나열 → 표 형식으로 변경 (가시성 문제로 사용자 피드백 반영), DM 재전송 |
| 2026-07-05 | 동일 | "Verish offline daily report (<날짜>)" 제목을 문서 맨 위로 승격, 기존 자리는 "📋 정성 액션 요약 (전체)"로 대체, 전체 통합본 DM 재전송 |
| 2026-07-05 | 동일 | daily 경량화: 판매 급증 상품 매장별 breakdown 제거(전사 합산만 유지), 4개 카테고리 전부 무이슈인 매장은 "이상 없음" 한 줄로 압축 — 기준은 1절·3.2·4.4 참고 |
| 2026-07-05 | 2026-07-04 (정성+KPI 결합) | **첫 실제 채널 발행** (#part-offline-비공개, DM 아님). 발행 후 신제주 전환율 영향 항목에 "21시 이후 34.4%가 최고"라는 오기재 발견 → 스레드 정정 메시지 발행, 드래프트 파일도 수정 — [reports/daily-digest-2026-07-04-draft.md](reports/daily-digest-2026-07-04-draft.md). 처리 속도 이슈 피드백 반영해 6절에 성능 개선 메모 추가 |
| 2026-07-05 | 2026-07-04 (KPI 스크립트) | 전환율의 전주 대비 증감 표기를 상대 증감률(%) → 절대 %p 차이 기반 bps로 변경(예: 13.4%→16.8%는 "+340bps"). ⚠️ 플래그 판정 기준은 기존 상대 증감률(±25%) 그대로 유지, 표시 단위만 변경 — 기준은 3·3.1 참고. 다음 실제 발행부터 반영 |
| 2026-07-05 | 2026-07-04 (정성+KPI 결합) | 발행 구조를 "채널 메인 긴 메시지 1개" → "채널 메인 볼드 제목 1줄 + 스레드 답글(`@sales-offline` 태그 + 본문)"으로 변경(피드 길이 문제 피드백 반영). `sales-offline` 서브팀 ID(`S08ESRKCQLD`)는 과거 채널 메시지 검색으로 확인. DM(구성회 본인)으로 2단 구성 테스트 완료 후 확정 — 기준은 4.5·5절 참고 |
| 2026-07-06 | 2026-07-06 (정성+KPI 결합) | 채널 실제 발행 (안전 이슈 1건: 애월 피팅룸 커버 경상 사고, 복수 매장 공통 이슈: 쿨핏 브라 볼륨핏 로고 반복 품절 + Shopify 대만/홍콩 오버셀 크로스체크). 발행 후 사용자 피드백으로 daily 루틴에서 Shopify 데이터 제외 결정, VOC 원문 추출 스크립트화(`voc-for-date.ts`) 진행 — 아래 2026-07-07 항목 참고 |
| 2026-07-07 | - | **Shopify 데이터를 daily 루틴에서 제외** (2절·6절 크로스채널 검증 항목 반영) — 이후 daily는 순수 오프라인(PLAY MD+mAsh+Slack VOC)만 다룸. 처리 속도 개선을 위해 `npm run voc-for-date <날짜>` 신규 스크립트 추가([src/report/voc-for-date.ts](src/report/voc-for-date.ts)) — 날짜 필터+매장별 그룹핑+마감보고 원문 판별을 Bash 호출 1회로 처리, 기존 `node -e` 미리보기+임시파일 덤프+Read 3회 왕복을 대체 |
| 2026-07-08 | 2026-07-07 (정성+KPI 결합) | 채널 실제 발행 (`voc-for-date.ts` 스크립트 첫 실전 사용, 순조롭게 동작). 안전 이슈 2건(명동 화재경보기 오작동으로 고객 대피, 신제주 피팅룸 도어 오작동으로 고객 갇힘 사고) + 애월 자동문 반복 소음을 모니터링 필요 건으로 안전 카테고리에 함께 포함, 복수 매장 공통 이슈 3건(시그니처 PLUS 재고/구성, 올커버 브라 품절, 브라탑 소형 사이즈 품절) 식별 — [reports/daily-digest-2026-07-07-draft.md](reports/daily-digest-2026-07-07-draft.md) |
